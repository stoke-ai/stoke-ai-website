'use client';
import {useState} from 'react';
export default function ReviewLogin(){const[username,setUsername]=useState('braxton');const[code,setCode]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/morgan-door-review/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,code})});if(!r.ok){const b=await r.json().catch(()=>({}));setError(b.error||'Sign in failed.');setBusy(false);return;}location.reload();}return <form onSubmit={submit} className="loginForm"><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/></label><label>Access code<input type="password" value={code} onChange={e=>setCode(e.target.value)} autoComplete="current-password" autoFocus/></label>{error&&<p className="error" role="alert">{error}</p>}<button disabled={busy}>{busy?'Opening review…':'Open review'}</button></form>}
