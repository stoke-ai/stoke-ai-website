import {NextResponse}from 'next/server';import{isReviewAuthenticated}from '@/lib/morgan-door/auth';import{getCase}from '@/lib/morgan-door/store';import{refreshCase}from '@/lib/morgan-door/hcp';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!await isReviewAuthenticated())return NextResponse.json({error:'Sign in again.'},{status:401});const{id}=await params;const item=await getCase(id);if(!item)return NextResponse.json({error:'Case not found.'},{status:404});return NextResponse.json({case:await refreshCase(item)});}
