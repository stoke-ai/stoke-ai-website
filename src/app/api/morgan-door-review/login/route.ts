import { NextResponse } from 'next/server';
import { isSameOrigin,setReviewSession,verifyReviewLogin } from '@/lib/morgan-door/auth';
import { loginRateLimit } from '@/lib/morgan-door/store';
function clientKey(request:Request){const forwarded=request.headers.get('x-forwarded-for')?.split(',')[0]?.trim();return `review-login:${forwarded||request.headers.get('x-real-ip')||'unknown'}`;}
export async function POST(request:Request){if(!isSameOrigin(request))return NextResponse.json({error:'Request rejected.'},{status:403});const body=await request.json().catch(()=>null) as {username?:string;code?:string}|null;if(!body?.username||!body.code)return NextResponse.json({error:'Enter your username and access code.'},{status:400});const valid=verifyReviewLogin(body.username,body.code);const rate=await loginRateLimit(clientKey(request),valid);if(!rate.allowed)return NextResponse.json({error:'Too many attempts. Try again later.'},{status:429});if(!valid)return NextResponse.json({error:'Username or access code is incorrect.'},{status:401});await setReviewSession();return NextResponse.json({ok:true});}
