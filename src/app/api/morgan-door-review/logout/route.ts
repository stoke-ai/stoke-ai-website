import { NextResponse } from 'next/server';import { clearReviewSession,isSameOrigin } from '@/lib/morgan-door/auth';export async function POST(request:Request){if(!isSameOrigin(request))return NextResponse.json({error:'Request rejected.'},{status:403});await clearReviewSession();return NextResponse.redirect(new URL('/morgan-door-review',request.url),303);}
